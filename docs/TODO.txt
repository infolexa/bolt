h1. Todo for Monday Feb 1, 2010

h3. Widget and Pages visibility check.

* Change @User::belongsto()@ in Widget class. Make it @Auth::logged_in(array $roles)@ - DONE
* In the Widget class, check if "guest" is in the usergroups, return true if User is not logged in. - DONE

h3. Anchors

* html::anchor helper should generate route using the reverse routing.- DONE

h3. Registration